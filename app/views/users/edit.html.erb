<div class="container">
  <div class="jumbotron">
    <div class="form-group">
      <%= form_for @user do |f| %>
        <%= render :partial => 'form', :locals => {:f => f} %>
        <%= f.submit 'Update', :class => "btn btn-primary mt-2" %>
      <% end %>
    </div>
  </div>
</div>
